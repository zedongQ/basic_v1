<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.ieforex.dao.ECustomerLimitMapper">
   
	<select id="searchById" resultType="org.ieforex.entity.ECustomerLimit">
		select * from e_customer_limit where customer_id = #{customerId} ORDER BY operate_date DESC 
	</select>

</mapper>